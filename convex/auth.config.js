const authConfig = {
    providers: [
        {
            domain: "https://ample-mosquito-26.clerk.accounts.dev",
            applicationID: "convex",
        },
    ],
};

export default authConfig;
